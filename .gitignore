# Environment
.env
.venv/
__pycache__/
*.pyc
*.pyo
.DS_Store

# Data — raw and processed files are NOT committed to git
# (too large, regeneratable via scripts)
data/raw/
data/processed/

# External data can be committed if small (< 1MB)
# data/external/   ← intentionally not ignored

# Jupyter checkpoints
.ipynb_checkpoints/

# Build artifacts
dist/
*.egg-info/
